# QuoteTimeline — Project State

## Product (one-liner)
**QuoteTimeline** is a neutral, source-first index of verbatim public quotes with dates, topics, and links to primary sources.

Goal: scalable SEO traffic (quote pages + topic timelines) and ad monetization.

## Live links
- Site: https://quotetimeline.vercel.app
- GitHub repo: https://github.com/maoryadin/quotetimeline

## Current stack
- Next.js (App Router) + TypeScript
- Tailwind (via `@import "tailwindcss"` in `globals.css`)
- Deployed on Vercel
- Data is currently **sample data** in `src/lib/sample-data.ts` (must be replaced before public launch/monetization)

## Key pages/routes
- `/` — landing page with hero + topics + latest quotes
- `/search` — keyword search (MVP: searches sample data in-memory)
- `/person/[person]` — person hub (currently `donald-trump`)
- `/topic/[topic]` — topic hub
- `/quote/[slug]` — individual quote page
- `/trending` — topic frequency snapshot
- `/sitemap.xml` + `/robots.txt` — generated by Next

## UX/UI work already done
- Sticky header nav (Home/Trending/Trump/Search)
- New hero section, nicer cards, gradients
- Shared footer with disclaimer

## Compliance / safety notes
- Site positioning is **neutral/informational**.
- Disclaimer is present in UI: **not affiliated / not endorsed**.
- Avoid official logos/branding; always link to primary sources.

## Backlog
See `BACKLOG.md` for an ordered, maintained backlog.

## Immediate TODO (highest ROI)
### 1) Real data ingestion (SEO engine)
- Pick a first source (transcripts/official speeches/interviews).
- Build an ingest script to normalize records into a stable schema.
- Store in a real DB (Postgres) + add search indexing (Postgres FTS or Meilisearch).
- Generate many quote pages + topic pages; ensure stable slugs + canonical URLs.

### 2) Information architecture (better navigation)
- Add filters: year, source type, topic.
- Add pagination/infinite scroll on person/topic pages.
- Add “timeline view” per topic.

### 3) Monetization readiness
- Add About / Sources / Contact pages.
- Set up AdSense after content is real + policies are satisfied.
- Add basic analytics (Plausible/GA4) + Core Web Vitals checks.

## Notes for future sessions (how to continue fast)
- Start by reading this file (`PROJECT.md`).
- Next file to touch for data: `src/lib/sample-data.ts` → replace with DB-backed model.
- If Vercel URL changes, update `NEXT_PUBLIC_SITE_URL` env var.
